---
name: bounded-task
description: 处理已确认边界、低风险、局部且可直接验收的小型实现任务时使用；共享框架/契约、工作流、schema、IAP、安全与跨模块决策不触发。
---

# Bounded Task

1. 从请求与相邻代码确定局部范围和可观察结果，直接实施；“确认边界”不表示必须向用户提问。
2. 复用现有实现和测试，只增加能暴露本次行为错误的检查；不为小改动建立公共抽象或新的执行层。
3. 默认由当前代理直接完成。仅用户要求时委派，提供精确范围、硬约束与验收，收回后核验实际产物。
